name: Generate README TOC

on:
  push:
    branches:
      - 'master'
    paths:
      - 'readme/README.md'

jobs:
  generateTOC:
    name: Generate TOC
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v4
